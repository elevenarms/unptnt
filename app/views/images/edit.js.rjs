
page.replace_html 'imagediv', :partial => 'edit',
      :locals => { :project_id => @project.id, @image => nil,
                   :item_id => @item.id }
page.visual_effect :highlight, 'imagediv', :duration => 1    
                 