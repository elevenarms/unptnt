
page.replace_html "imagediv", :partial => "items/show_image",
                :locals => { :bom => @bom, :item => @item, :project => @project,
                             :logged_in => @logged_in, :current_user_is_editor => @current_user_is_editor }
page.visual_effect :highlight, "imagediv", :duration => 1
