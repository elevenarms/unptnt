<h1>New bom for project <%=  @project.name %></h1>

<% form_for([@project, @bom]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label:name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label(@bom, :description) %><br />
    <%= f.text_field :description %>
  </p>
  
  <p>
    <%= f.submit "Create bom" %>
  </p>
<% end %>

<%= link_to 'Back to project', project_path(@project) %>
