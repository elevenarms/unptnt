The forum for this
<%= @forum.subject_type %>
has not been started yet.<br/>

<% form_for :forum, :url => { :controller => 'forums', :action => 'create', :id => @forum.id } do |f| %>
  <%= hidden_field_tag :forum_id, @forum.id %>
  <%= hidden_field_tag :project_id, @project.id %>
  <%= hidden_field_tag :item_id, @item.id %>
  <%= f.submit 'Create the first topic and post' %>
<% end %><br/>
or
<% if @forum.subject_type == 'item' then %>
  <%= link_to "return to item", bom_item_path(@item.bom, @item)  %>
<% else %>
  <%= link_to "return to project", project_path(@project)  %>
<% end %>
